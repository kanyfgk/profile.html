const VAERO_BUILD = {
    id: "build-005",
    date: "2026-07-08",
    note: "actions v2 loaded"
};

window.VAERO_BUILD = VAERO_BUILD;

console.log(
    "VAERO BUILD:",
    VAERO_BUILD.id,
    VAERO_BUILD.date,
    VAERO_BUILD.note
);
